<!DOCTYPE html>
<html lang="en-US">
  <head>
    <title>{{title}}</title>
    {% include head.liquid %}
  </head>
  <body>
    {% include nav.liquid %}

    <main>
      {{ content }}
      {{ collection.posts.length }}
      {% assign postslist = collection.posts %}

      {% include components/postslist.liquid %}
    </main>

    {% include footer.liquid %}
  </body>
</html>
